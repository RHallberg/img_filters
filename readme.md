Simple awk image filter that takes farbfeld image and converts them pixel by pixel. Requires farbfeld.


## Usage

```bash
./transform your_image.png filter_name
```

resulting image is suffixed with .out.png

